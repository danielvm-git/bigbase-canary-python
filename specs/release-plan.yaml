release:
  version: "0.1.0"  # mirror only — authority is `gh release view` / git tags once big-release runs
  bump_hint: minor
  codename: canary-genesis

epics:
  - id: e01
    title: Canary site scaffold
    wsjf: 1
    bcps_total: null
    stories:
      - e01s01
